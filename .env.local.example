# Copy to .env.local for local dev. In Vercel, set these under
# Project → Settings → Environment Variables instead.

# Required for the contact form to send email — get a key from resend.com
RESEND_API_KEY=

# Optional — defaults shown
# CONTACT_TO_EMAIL=info@tomukun.com
# CONTACT_FROM_EMAIL=Tomukun Website <onboarding@resend.dev>
